/**
 * Selection-review overlay card.
 *
 * STUB — scaffolded in Task A5 (window + entry wiring only). The real UI (action chips, ReviewPanel,
 * Apply/Copy/Close, keyboard handling, review-text/review-hide listeners) is built in Task A8.
 */
export default function ReviewOverlay() {
  return <div className="review-overlay-root" />;
}
